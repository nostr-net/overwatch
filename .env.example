# Force the application to use only this specific relay
# When set, all relay selection UI will be disabled and the app will only connect to this relay
# Example: VITE_FORCED_RELAY=wss://nos.lol
#
# For local development with a relay on the host machine:
# VITE_FORCED_RELAY=ws://localhost:7777
# VITE_FORCED_RELAY=

# Proxy server URL (for Docker builds)
JUMBLE_PROXY_SERVER_URL=http://localhost:8090

# Social URL (for CORS configuration)
JUMBLE_SOCIAL_URL=http://localhost:8089

# GitHub token for proxy server (optional)
JUMBLE_PROXY_GITHUB_TOKEN=
